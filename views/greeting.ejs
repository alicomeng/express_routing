<% include partials/header %>
    <header>
      <h1>Thanks a million times..</h1>
    </header>
    <div id="container">
      <p>This home page is being served from the <span>views</span> folder using
        <span>EJS</span> template engine, which was installed using utilizing a npm
      install in the command line</p>
      <h3>Here is a list of the greetings you deserve:</h3>
      <hr>
      <p><%= greetings.toUpperCase() %></p>

      <%  if(msg.toLowerCase() === 'soso') { %>
          <p>You wrote my name</p>
      <% } %>
      <h2>Books: </h2>
      <ul>
        <% for(var i = 0; i < posts.length; i++){ %>
          <li>
            <%= posts[i].title %> - <em>By: </em> <strong><%= posts[i].author %></strong>
          </li>
        <% } %>

        <li>Bellow are the same itmes generated by "forEach" loop</li>

        <% posts.forEach(function(book){ %>
          <li>
            <%= book.title %> - <em>By: </em> <strong><%= book.author %></strong>
          </li>

        <% }) %>
      </ul>
    </div>

<% include partials/footer %>
